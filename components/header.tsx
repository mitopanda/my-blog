import type { FC } from 'react';
import { VStack, Heading } from '@chakra-ui/react';

export const Header: FC = () => {
  return (
    <VStack>
      <Heading>mishitoshi's blog</Heading>
    </VStack>
  );
};
